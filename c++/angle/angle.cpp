#include <iostream>
#include <cmath>


using namespace std;

int main()
{
    cout << atan2(2, 1) * 180 / M_PI << endl;
    cout << asin(sqrt(5) / 5) * 180 / M_PI << endl;
    cout << asin(sqrt(3) / 3) * 180 / M_PI << endl;
    return 0;
}
